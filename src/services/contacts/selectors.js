/** @format */

export const selContacts = (state) => state.contacts.items;
export const selFilter = (state) => state.contacts.filter;
export const selModal = (state) => state.contacts.modal;
export const selEditContact = (state) => state.contacts.editItem;
